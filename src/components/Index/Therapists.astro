---
import { Image } from "astro:assets";
import SalleA from "@assets/images/Rooms/SalleA.jpg";
import SalleB from "@assets/images/Rooms/SalleB.jpg";
import Formation from "@assets/images/Rooms/Formation.jpg";
import Pilates from "@assets/images/Rooms/Pilates.jpg";
---

<section class="therapists-section">
  <div class="content-row">
    <div class="text-col">
      <h2>Vous êtes thérapeutes ?</h2>
      <p>J'ai à coeur de partager ce lieu avec vous.</p>
      <p>
        Soyons nombreux à accueilir celles et ceux qui sont en demande. Soyons
        nombreux pour échanger et partager. Offrez vos pratiques, vos
        connaissances, votre sensibilité...
      </p>
    </div>
    <div class="images-col">
      <div class="image-wrapper">
        <Image src={SalleA} alt="Cabinet de thérapie" />
      </div>
      <div class="image-wrapper">
        <Image src={SalleB} alt="Espace de consultation" />
      </div>
    </div>
  </div>

  <div class="content-row">
    <div class="text-col">
      <h2>Salle formations</h2>
      <p>
        Il vous est possible de réserver le local pour vos formations, vos
        conférences, vos évènements...
      </p>
    </div>
    <div class="image-col">
      <Image src={Formation} alt="Cabinet ostéopathe" />
    </div>
  </div>

  <div class="content-row">
    <div class="text-col">
      <h2>Espace Yoga</h2>
      <p>
        Si vous êtes professeur de Yoga Pilates, l'espace Yoga peut être mis à
        votre disposition.
      </p>
    </div>
    <div class="image-col">
      <Image src={Pilates} alt="Cabinet partagé" />
    </div>
  </div>
</section>

<style lang="scss">
  @use "@styles/config/variables" as *;
  @use "@styles/config/mixins" as *;

  .therapists-section {
    width: 100%;
    margin: 4rem auto;
    display: flex;
    flex-direction: column;
    gap: 3rem;
    background-color: var(--white-bg-blur);
  }

  .content-row {
    display: flex;
    flex-direction: column;
    gap: 2rem;

    .text-col {
      text-align: center;
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 1rem;
      padding: 1rem;

      h2 {
        color: var(--important-color);
        margin-bottom: 1rem;
      }
    }

    .images-col {
      display: flex;

      .image-wrapper {
        flex: 1;
        height: 300px;
        overflow: hidden;

        img {
          width: 100%;
          height: 100%;
          object-fit: cover;
        }
      }
    }

    .image-col {
      height: 300px;
      overflow: hidden;

      img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
    }
  }

  @include breakpoint-up($breakpoint-desktop) {
    .therapists-section {
      gap: 0;
      margin: 10rem auto; // Remove margin to be full width if needed, or keep vertical margin
      max-width: 100%; // Ensure full width
    }

    .content-row {
      flex-direction: row;
      align-items: stretch;
      padding: 0;
      gap: 0;
      height: 50vh; // Limit height as requested
      max-height: 500px; // Cap it to ensure it doesn't get too tall

      // Target the second row to swap visual order on desktop
      &:nth-child(2) {
        flex-direction: row-reverse;
      }

      .text-col {
        width: 50%;
        padding: 4rem; // Keep internal padding for text readability
        background-color: rgba(
          255,
          255,
          255,
          0.5
        ); // Optional: subtle background to differentiate if needed
      }

      .image-col {
        width: 50%;
        height: 100%;
        border-radius: 0; // Remove border radius
        padding: 0; // No padding
      }

      .images-col {
        width: 50%;
        padding: 0;
        gap: 0; // No gap between images

        .image-wrapper {
          border-radius: 0;
          height: 100%;

          img {
            width: 100%;
            height: 100%;
            object-fit: cover;
          }
        }
      }
    }
  }
</style>
